server:
  port: 19100

eureka:
  client:
    register-with-eureka: false
    fetch-registry: false
    service-url:
      defaultZone: http://localhost:19100/eureka/
  instance:
    hostname: localhost